<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<AnchorPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.travelagency.travelagency.controllers.ClientController"
            style="-fx-background-color: #f0f4f7; -fx-padding: 20;">

    <!-- Container principal horizontal -->
    <HBox spacing="20" AnchorPane.topAnchor="0" AnchorPane.bottomAnchor="0" AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0">
        <!-- Partie gauche : liste des clients -->
        <VBox spacing="15" >
            <Label text="Liste des Clients 📊 "
                   style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #0072ff;"/>

            <TableView fx:id="clientsTable" prefHeight="580" style="-fx-background-color: white; -fx-border-color: #d0d0d0; -fx-border-radius: 5; -fx-background-radius: 5;">
                <columns>
                    <TableColumn fx:id="colNom" text="Nom" prefWidth="200"/>
                    <TableColumn fx:id="colEmail" text="Email" prefWidth="250"/>
                    <TableColumn fx:id="colTelephone" text="Téléphone" prefWidth="150"/>
                    <TableColumn fx:id="colFrequence" text="Fréquence" prefWidth="100"/>
                </columns>
            </TableView>
        </VBox>

        <!-- Partie droite : espace réservé ou panneau -->
        <VBox spacing="10"  style="-fx-background-color: #ffffff; -fx-border-color: #d0d0d0; -fx-border-radius: 5; -fx-background-radius: 5; -fx-padding: 15;">
            <Label text="Détails / Actions" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #333;"/>
            <!-- Ajoute ici des composants, boutons, ou autres éléments -->
            <Button text="Ajouter un client" style="-fx-background-color: #0072ff; -fx-text-fill: white; -fx-font-weight: bold;"/>
            <Button text="Supprimer" style="-fx-background-color: #ff4d4d; -fx-text-fill: white;"/>
            <Separator/>
            <Label text="Informations complémentaires" style="-fx-font-size: 14px; -fx-text-fill: #555;"/>
            <!-- Placeholder pour contenu supplémentaire -->
            <TextArea promptText="Détails ou notes..." style="-fx-border-color: #ccc; -fx-border-radius: 3; -fx-background-radius: 3;"/>
        </VBox>
    </HBox>
</AnchorPane>
